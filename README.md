# Harugeki Studio

An editor for the `data/*.bin` archives of *Suzumiya Haruhi no Gekitou* (2007).
Browse an archive, view models in a hardware 3D viewport and textures in 2D,
export to glTF/PNG, and replace textures back into the archive.